<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.AdministrativemapMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Administrativemap">

        <result property="id" column="id"/>
        <result property="name" column="name"/>

        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />

    </resultMap>

    <insert id="addAdministrativemap" >

        insert into administrativemap(
        name,
        pictureurl,
        status,
        userid)
          values (
        #{name},
        #{pictureurl},
        #{status},
        #{userid})

    </insert>


    <select id="selectAdministrativemap" resultMap="result">

        select *  from administrativemap where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="selectAdministrativemap2" resultMap="result">

        select *  from administrativemap where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1
        and userid=#{userid}


    </select>

    <update id="delAdministrativemape">

        update administrativemap set status=-1 where id=#{id}   and userid=#{userid}

    </update>


    <update id="updateAdministrativemap">


        update  administrativemap  set name=#{name} , pictureurl=#{pictureurl} where id=#{id}   and userid=#{userid}

    </update>



    <select id="getAdministrativemapCount" resultType="integer">

        select count(*)  from administrativemap
        where
        <if test="name!=null">
            name like #{name} and

        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="getAdministrativemapById" resultMap="result">

        select *  from administrativemap where id=#{id} and status=1 and userid=#{userid}

    </select>


    <select id="getAdministrativemapById2" resultMap="result">

        select *  from administrativemap where id=#{id} and status=1    and userid=#{userid}

    </select>

    <select id="getAdministrativemapByMaxId" resultMap="result">

        select *  from administrativemap where id=(select max(id)  from administrativemap where status=1  and userid=#{userid}) and status=1  and userid=#{userid}

    </select>

</mapper>