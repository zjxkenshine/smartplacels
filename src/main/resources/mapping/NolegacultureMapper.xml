<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.NolegacultureMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Nolegaculture">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />
        <result property="abbreviation" column="abbreviation" />
    </resultMap>

    <insert id="addNolegaculture" >

        insert into nolegaculture(
        name,
         pictureurl,
          status,
          userid,
          abbreviation)
          values (
          #{name},
          #{pictureurl},
          #{status},
          #{userid},
          #{abbreviation})

    </insert>


    <select id="selectNolegaculture" resultMap="result">

        select *  from nolegaculture where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="selectNolegaculture2" resultMap="result">

        select *  from nolegaculture where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1
        and userid=#{userid}


    </select>

    <update id="delNolegaculture">

        update nolegaculture set status=-1 where id=#{id}   and userid=#{userid}

    </update>


    <update id="updateNolegaculture">


        update  nolegaculture  set
        name=#{name} ,
        pictureurl=#{pictureurl},
        abbreviation=#{abbreviation}

         where id=#{id}   and userid=#{userid}

    </update>



    <select id="getNolegacultureCount" resultType="integer">

        select count(*)  from nolegaculture
        where
        <if test="name!=null">
            name like #{name} and

        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="getNolegacultureById" resultMap="result">

        select *  from nolegaculture where id=#{id} and status=1 and userid=#{userid}

    </select>


    <select id="getNolegacultureById2" resultMap="result">

        select *  from nolegaculture where id=#{id} and status=1    and userid=#{userid}

    </select>

    <select id="getNolegacultureByMaxId" resultMap="result">

        select *  from nolegaculture where id=(select max(id)  from nolegaculture where status=1  and userid=#{userid}) and status=1  and userid=#{userid}

    </select>

</mapper>