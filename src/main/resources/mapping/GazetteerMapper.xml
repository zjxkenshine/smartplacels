<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.GazetteerMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Gazetteer">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />
    </resultMap>

    <insert id="addGazetteer" >

        insert into gazetteer(
        name,
        pictureurl,
        status,
        userid)
          values
          (#{name},
          #{pictureurl},
          #{status},
          #{userid})

    </insert>


    <select id="selectGazetteer" resultMap="result">

        select *  from gazetteer where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="selectGazetteer2" resultMap="result">

        select *  from gazetteer where

        <if test="name!=null">
            name like #{name} and
        </if>
        status=1
        and userid=#{userid}


    </select>

    <update id="delGazetteer">

        update gazetteer
        set status=-1 where id=#{id}   and userid=#{userid}

    </update>


    <update id="updateGazetteer">


        update  gazetteer
         set name=#{name} ,
          pictureurl=#{pictureurl}
          where id=#{id}
          and userid=#{userid}

    </update>



    <select id="getGazetteerCount" resultType="integer">

        select count(*)  from gazetteer
        where
        <if test="name!=null">
            name like #{name} and

        </if>
        status=1

        and userid=#{userid}

    </select>


    <select id="getGazetteerById" resultMap="result">

        select *  from gazetteer
         where id=#{id} and status=1 and userid=#{userid}

    </select>


    <select id="getGazetteerById2" resultMap="result">

        select *  from gazetteer where id=#{id} and status=1    and userid=#{userid}

    </select>

    <select id="getGazetteerByMaxId" resultMap="result">

        select *  from gazetteer where id=(
        select max(id)  from gazetteer where status=1  and userid=#{userid}) and status=1  and userid=#{userid}

    </select>

</mapper>