<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.OcplacenamesMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Ocplacenames">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />
        <result property="abbreviation" column="abbreviation" />
    </resultMap>

    <insert id="addOcplacenames" >

        insert into ocplacenames(
        name,
        pictureurl,
        status,
        userid,
        abbreviation

        )
          values (
          #{name},
          #{pictureurl},
          #{status},
          #{userid},
          #{abbreviation})

    </insert>


    <select id="selectOcplacenames" resultMap="result">

        select *  from ocplacenames where

        <if test="name!=null">

            name like #{name} and

        </if>
        status=1

        and

        userid=#{userid}

        order by id desc


    </select>


    <select id="selectOcplacenames2" resultMap="result">

        select *  from ocplacenames where

        <if test="name!=null">

            name like #{name} and
        </if>
        status=1

        and userid=#{userid}



    </select>




    <update id="delOcplacenames">

        update ocplacenames set status=-1 where id=#{id}   and userid=#{userid}

    </update>


    <update id="updateOcplacenames">


        update  ocplacenames  set
        name=#{name} ,
         pictureurl=#{pictureurl} ,
         abbreviation=#{abbreviation}

         where id=#{id}   and userid=#{userid}

    </update>


    <select id="getOcplacenamesCount" resultType="integer">


        select count(*)  from ocplacenames

        where

        <if test="name!=null">
            name like #{name} and

        </if>
         userid=#{userid}
        and
        status=1


    </select>



    <select id="getOcplacenamesById" resultMap="result">

        select *  from ocplacenames where id=#{id} and status=1 and userid=#{userid}

    </select>

    <select id="getOcplacenamesById2" resultMap="result">

        select *  from ocplacenames where id=#{id} and status=1    and userid=#{userid}

    </select>

    <select id="getOcplacenamesByMaxId" resultMap="result">

        select *  from ocplacenames where id=(select max(id)  from ocplacenames where status=1  and userid=#{userid}) and status=1  and userid=#{userid}

    </select>

</mapper>