<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.ChafoodMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Chafood">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />

    </resultMap>

    <insert id="addChafood" >

        insert into chafood(name, pictureurl, status,userid)  values (#{name},#{pictureurl},#{status},#{userid})

    </insert>


    <select id="selectChafoodByName" resultMap="result">

        select *  from chafood

        where
        <if test="name!=null">
            name like #{name} and
        </if>

        status=1

        and userid=#{userid}


    </select>


    <select id="selectChafoodByName2" resultMap="result">

        select *  from chafood

        where
        <if test="name!=null">
            name like #{name} and
        </if>

        status=1

        and userid=#{userid}


    </select>


    <update id="delChafood">

        update chafood set status=-1 where id=#{id}    and userid=#{userid}

    </update>


    <update id="updateChafood">


        update  chafood set name=#{name} , pictureurl=#{pictureurl} where id=#{id}    and userid=#{userid}

    </update>


    <select id="getChafoodCount" resultType="integer">


        select count(*)  from chafood

        where

        <if test="name!=null">
            name like #{name} and

        </if>

        status=1
        and userid=#{userid}

    </select>



    <select id="getChafoodById" resultMap="result">

        select *  from chafood where id=#{id} and status=1 and userid=#{userid}

    </select>


    <select id="getChafoodById2" resultMap="result">

        select *  from chafood where id=#{id} and status=1     and userid=#{userid}

    </select>

    <select id="getChafoodByMaxId" resultMap="result">

        select *  from chafood where id=(select max(id)  from chafood where status=1  and userid=#{userid})

         and status=1   and userid=#{userid}

    </select>


</mapper>