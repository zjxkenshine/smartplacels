<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.StreetplanaMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Streetplana">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="userid" column="userid" />
        <result property="type" column="type" />
        <result property="abbreviation" column="abbreviation" />

    </resultMap>

    <insert id="addStreetplana" >

        insert into streetplana(
        name,
        pictureurl,
        status,
        userid,
        type,
        abbreviation

        )  values (
        #{name},
        #{pictureurl},
        #{status},
        #{userid},

        #{type},
        #{abbreviation}

        )

    </insert>


    <select id="selectStreetplana" resultMap="result">

        select *  from streetplana where

        <if test="name!=null">
            name like #{name} and

        </if>
        status=1

        and userid=#{userid}


    </select>


    <select id="selectStreetplana2" resultMap="result">

        select *  from streetplana where

        <if test="name!=null">
            name like #{name} and

        </if>

        <if test="type!=null">
            type=#{type} and

        </if>

        status=1
        and userid=#{userid}




    </select>


    <update id="delStreetplana">

        update streetplana set status=-1 where id=#{id}   and userid=#{userid}

    </update>


    <update id="updateStreetplana">

        update  streetplana  set
        name=#{name} ,
        pictureurl=#{pictureurl},
        type=#{type},
        abbreviation=#{abbreviation}

         where id=#{id}  and userid=#{userid}

    </update>



    <select id="getStreetplanaCount" resultType="integer">


        select count(*)  from streetplana

        where

        <if test="name!=null">
            name like #{name} and

        </if>

        <if test="type!=null">
            type =#{type} and

        </if>

        status=1

        and userid=#{userid}

    </select>



    <select id="getStreetplanaById" resultMap="result">

        select *  from streetplana where id=#{id} and status=1 and userid=#{userid}

    </select>

    <select id="getStreetplanaById2" resultMap="result">

        select *  from streetplana where id=#{id} and status=1     and userid=#{userid}

    </select>

    <select id="getStreetplanaByMaxId" resultMap="result">

        select *  from streetplana where id=(select max(id)  from streetplana where status=1  and userid=#{userid} )

         and status=1     and userid=#{userid}

    </select>

</mapper>