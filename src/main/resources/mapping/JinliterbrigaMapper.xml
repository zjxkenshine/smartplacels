<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csdl.smartplacenew.mapper.JinliterbrigaMapper">
    <resultMap id="result" type="com.csdl.smartplacenew.information.Jinliterbriga">
        <result property="id" column="id"/>
        <result property="name" column="name" />
        <result property="pictureurl" column="pictureurl" />
        <result property="status" column="status" />
        <result property="type" column="type" />
        <result property="userid" column="userid" />

    </resultMap>

    <insert id="addJinliterbriga" >

        insert into jinliterbriga(name, type,pictureurl, status,userid)  values (#{name},#{type},#{pictureurl},#{status},#{userid})

    </insert>


    <select id="selectJinliterbriga" resultMap="result">

        select *  from jinliterbriga where

        <if test="name!=null">
            name like #{name} and
        </if>
        type=#{type} and

        status=1

        and userid=#{userid}


    </select>


    <select id="selectJinliterbriga2" resultMap="result">

        select *  from jinliterbriga where

        <if test="name!=null">
            name like #{name} and
        </if>
        type=#{type} and

        status=1
and userid=#{userid}



    </select>


    <select id="selectAllJinliterbriga" resultMap="result">

        select *  from jinliterbriga where status=1

    </select>

    <update id="delJinliterbriga">

        update jinliterbriga set status=-1 where id=#{id}    and userid=#{userid}

    </update>


    <update id="updateJinliterbriga">


        update  jinliterbriga  set name=#{name} , pictureurl=#{pictureurl} where id=#{id}   and userid=#{userid}

    </update>


    <select id="getJinliterbrigaCount" resultType="integer">


        select count(*)  from jinliterbriga

        where

        <if test="name!=null">
            name like #{name} and

        </if>
        type=#{type} and

        status=1

        and

        userid=#{userid}


    </select>



    <select id="getJinliterbrigaById" resultMap="result">

        select *  from jinliterbriga where id=#{id} and status=1 and userid=#{userid}


    </select>


    <select id="getJinliterbrigaById2" resultMap="result">

        select *  from jinliterbriga where id=#{id} and status=1    and

        userid=#{userid}


    </select>

    <select id="getJinliterbrigaByMaxId" resultMap="result">

        select *  from jinliterbriga
         where id=(select max(id)  from jinliterbriga where status=1
         and userid=#{userid}) and status=1
          and
        userid=#{userid}


    </select>


    <select id="getJinliterbrigaByNameAndUserid" resultMap="result">

        select *  from jinliterbriga where

        <if test="name!=null">

            name like #{name} and

        </if>
        status=1

        and

        type=#{type} and

        userid=#{userid}

        order by id asc


    </select>

</mapper>